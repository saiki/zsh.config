[ -s "$HOME/.zsh/config.zsh" ] && \. $HOME/.zsh/config.zsh
[ -s "$HOME/.zsh/functions.zsh" ] && \. $HOME/.zsh/functions.zsh
[ -s "$HOME/.zsh/nvm.zsh" ] && \. $HOME/.zsh/nvm.zsh
[ -s "$HOME/.zsh/google_cloud_sdk.zsh" ] && \. $HOME/.zsh/google_cloud_sdk.zsh
